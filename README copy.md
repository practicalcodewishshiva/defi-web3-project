## Installation

1. Clone the repository
2. Install the dependencies on the project folder: `npm install`

## Usage

1. Run the development server: `npm start`
2. Access GamerHub in your web browser: `http://localhost:8080`# defi-web3-project
